# Copie de travail locale du Cloudflare Worker (déployé séparément sur Cloudflare,
# ce fichier n'est pas la source de vérité et ne doit pas être versionné ici).
cloudflare-worker.js

# Instructions locales pour les agents de développement.
/AGENTS.md

# PDF : ne versionner que les annales.
*.pdf
!/pdfs/annales/*.pdf
!/pdfs/annales/**/*.pdf

# Bibliothèque locale : ne pas versionner ces dossiers.
/pdfs/exercices/
/pdfs/livres/
